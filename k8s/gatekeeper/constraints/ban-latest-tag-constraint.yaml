apiVersion: constraints.gatekeeper.sh/v1beta1
kind: BanLatestTag
metadata:
  name: ban-latest-tag
spec:
  match:
    kinds:
      - apiGroups: ["apps"]
        kinds: ["Deployment"]
    namespaces:
      - sentinelai-dev
      - sentinelai-staging
      - sentinelai-prod